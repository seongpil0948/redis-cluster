# Example environment variables for redis-backup-tool and docker-compose helpers

# S3 destination for backups (required by helper targets)
# e.g., s3://my-bucket/redis-backups
S3_URI=

# Local directory to store temporary backup files
BACKUP_DIR=./backups

# When using shared credentials (~/.aws), set the profile name
# AWS_PROFILE=toy-root

# Prefer shared credentials/profile (mount ~/.aws and set AWS_PROFILE, AWS_SDK_LOAD_CONFIG=1)

# Compose-related
IP=0.0.0.0

# ECR push settings
# ECR_REGISTRY=123456789012.dkr.ecr.ap-northeast-2.amazonaws.com
# ECR_REPO=util/redis-backup-tool
# ECR_REGION=ap-northeast-2
